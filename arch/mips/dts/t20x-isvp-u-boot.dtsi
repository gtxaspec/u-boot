// SPDX-License-Identifier: GPL-2.0+
#include "t20-isvp-uboot-common.dtsi"

#if defined(CONFIG_SPL_MMC)
/ {
	/* MSC/SD boot config (isvp_t20x_msc0) reuses the SFC-NOR board .dts. */
	model = "Ingenic ISVP-T20X (MMC0)";
};
#endif
